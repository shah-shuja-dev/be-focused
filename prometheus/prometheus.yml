global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "focusguard"
    static_configs:
      # FastAPI running natively on Windows host
      - targets: ["host.docker.internal:8000"]
    metrics_path: /metrics